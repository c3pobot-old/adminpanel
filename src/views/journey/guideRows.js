import React, {Fragment} from 'react'

export default function GuideRows({opts={}, guides=[], guideUnits=[], selectGuide}){
  return(
    <Fragment>
    {guides?.length > && guides.map((guide, index)=>(
      
    ))}
    </Fragment>
  )
}
